# Flutter
app/.dart_tool/
app/build/
app/.flutter-plugins
app/.flutter-plugins-dependencies
# Drift generated code is regenerated via build_runner:
app/lib/**/*.g.dart
# Host-test native sqlite (downloaded per machine)
app/sqlite3.dll

# Design docs (specs/plans) — kept local, not committed:
docs/

# Go
server/server.exe
server/*.exe
